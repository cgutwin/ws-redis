{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/node": "^17.0.23",
    "lerna": "^4.0.0",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "init": "lerna run clean-lib --parallel && lerna run tsc --parallel"
  }
}
